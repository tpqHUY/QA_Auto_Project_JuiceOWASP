# Base URL of the Juice Shop instance under test.
# Local Docker default:
BASE_URL=http://localhost:3000

# Set to 1 in CI to enable retries, GitHub reporter, etc. (GitHub Actions sets this automatically)
# CI=1
